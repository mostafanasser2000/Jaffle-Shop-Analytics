models:
  - name: stg_products
    description: Product (food and drink items that can be ordered) data with basic cleaning and transformation applied
    columns:
      - name: product_id
        tests:
          - unique
          - not_null